import "./reset.css";
import "./style.css";

window.addEventListener("DOMContentLoaded", () => {
    // Create header
    const header = document.createElement("div");
    header.classList.add("header");

    document.body.pepend(header);
});
